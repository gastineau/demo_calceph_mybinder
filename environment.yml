name: example-environment
channels:
  - conda-forge
dependencies:
  - cython
  - numpy
  - setuptools
  - matplotlib
  - jupyter
  - pip
  - pip:
    - datetime
    - calcephpy
    - ipywidgets
    - convertdate
    - widgetsnbextension
